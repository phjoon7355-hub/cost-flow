# 회계원리 학습센터 문제팩 제작 기준

이 문서는 새 단원의 OCR 연습문제를 전산회계 1급 학습센터용 문제팩으로 바꿀 때 사용하는 작업 기준이다. 새 대화에서 이 문서만 전달해도 **교재용 문제팩과 AI 제작 문제팩을 함께 만들 수 있도록** 필요한 형식·출처·검증 기준을 모두 담는다.

작업 폴더:

`C:\Users\MyPC\Documents\Codex\2026-07-23\ocr-html-01-500-000-02\outputs\회계원리 학습센터`

파일 보관 위치:

- 문제팩 JSON 및 OCR 복원검토 문서: `C:\Users\MyPC\Documents\Codex\2026-07-23\ocr-html-01-500-000-02\outputs\회계원리 학습센터\문제펙`
- 사용자가 학습센터에서 내려받은 학습기록 JSON: `C:\Users\MyPC\Documents\Codex\2026-07-23\ocr-html-01-500-000-02\outputs\회계원리 학습센터\학습기록`

폴더명은 사용자가 만든 실제 폴더명인 `문제펙`을 그대로 사용한다. 문제팩을 새로 만들 때는 반드시 `문제펙` 폴더에 저장한다.

현재 학습센터 HTML을 수정해야 하는 요청이 아니라면, 문제팩 JSON과 OCR 검토 문서만 만든다. 학습기록 JSON은 학습센터에서 사용자가 직접 저장하는 파일이므로 문제 제작 작업에서 수정하지 않는다.

## 파일 저장 규칙

- 교재 문제팩: `문제펙\[단원명]_문제팩_교재.json`
- GPT 문제팩: `문제펙\[단원명]_문제팩_GPT.json`
- Claude 문제팩: `문제펙\[단원명]_문제팩_Claude.json`
- OCR 복원검토 문서: `문제펙\[단원명]_OCR_복원검토.md`
- 학습기록: 사용자가 Chrome에서 JSON 저장 후 수동으로 `학습기록` 폴더에 이동한다.

Chrome의 다운로드 폴더는 HTML이 강제로 지정할 수 없으므로, 문제 제작자가 학습기록 파일을 직접 만들거나 옮기려 하지 않는다. 학습기록은 사용자가 보관한 파일을 이후 분석 자료로 사용한다.

## 사용자가 새 대화에서 보낼 요청

다음처럼 요청하면 된다.

> 이 `문제팩_제작_기준.md`를 작업 기준으로 읽어 주세요. 아래에 전달하는 [단원명] 교재 OCR 연습문제를 복원·검토한 뒤, **교재 문제팩**과 **GPT 문제팩**을 각각 만들어 주세요. 교재 문제팩에는 복원한 교재 문제만, GPT 문제팩에는 새로 만든 기본·응용문제만 넣어 주세요. 기존 HTML은 수정하지 말고 UTF-8 JSON 문제팩과 OCR 복원검토 문서만 저장해 주세요.

가능하면 함께 알려 줄 정보:

- 단원명: 예) 재고자산
- 자료 출처: 교재 OCR, GPT 문제, Claude 문제 등
- 만들 문제팩: 예) `교재 문제팩 + GPT 문제팩`, 또는 `교재 문제팩 + Claude 문제팩`
- 원하는 AI 제작 기본문제 수와 변형문제 수
- 원본 이미지가 있는지 여부

그 아래에 OCR 텍스트를 붙인다.

## 반드시 구분할 두 문제팩

교재 OCR을 받았을 때는 기본적으로 아래 두 파일을 만든다. 두 파일을 한 JSON으로 합치지 않는다.

| 구분 | 목적 | 파일명 예시 | 출처(`source`) | 문제 구성 |
|---|---|---|---|---|
| 교재용 | 훼손된 교재 원문을 정확히 복원하여 원래 문제를 풀기 | `투자자산_문제팩_교재.json` | `교재 문제팩` | 복원한 교재 문제만. 원문 자체의 변형문제는 사용자가 요청할 때만 추가 |
| AI 제작용 | 같은 단원을 충분히 연습하고 응용하기 | `투자자산_문제팩_GPT.json` | `GPT 문제팩` 또는 `Claude 문제팩` | 새 기본문제와 각 기본문제에 연결한 변형 응용문제 |

- 교재용 문제의 표현·수치·선택지는 OCR 오류를 바로잡는 최소 범위에서만 수정한다. 정답이 하나가 되도록 보완한 내용은 OCR 복원검토 문서에 반드시 남긴다.
- AI 제작용 문제는 교재 문제의 보기 순서만 바꾸거나 숫자만 바꾼 문제가 아니어야 한다. 다른 상황·수치·질문 방향을 사용해 독립적인 연습문제로 만든다.
- 교재용과 AI 제작용은 같은 개념을 다뤄도 ID, 파일, `source`를 분리한다. 그래야 학습센터에서 출처별로 선택하여 풀 수 있고 오답 기록도 섞이지 않는다.
- 새 ID를 정하기 전에는 작업 폴더의 **모든 기존 문제팩 JSON ID를 먼저 검사**한다. 다른 단원이 이미 같은 약어를 썼다면 더 구체적인 접두어를 사용한다. 예: 투자자산은 기존 `INV-*`가 사용 중이면 `TIA-GPT-001`처럼 새 접두어를 사용한다.

## 작업 절차

### 1. OCR 원문 복원

- 깨진 글자, 숫자, 기호, 선택지 번호를 문맥과 회계 개념에 맞게 복원한다.
- OCR이 훼손한 부분을 임의로 확정하지 않는다.
- 숫자표·T계정·분개가 불명확하면 계산 가능한 구조를 먼저 복원하고, 추정한 부분을 검토 문서에 명시한다.
- 정답이 2개가 되거나 정답을 확정할 수 없는 문항은 그대로 문제팩에 넣지 않는다.
- 원본 이미지가 있으면 OCR 텍스트보다 이미지를 우선하여 대조한다.

### 2. 원문 검토 문서 작성

파일명:

`[단원명]_OCR_복원검토.md`

다음 내용을 포함한다.

- OCR 원문의 출처
- 문항별 복원 핵심
- 예상 정답과 간단한 근거
- 숫자 계산 과정
- OCR 때문에 확정하기 어려운 문항과 적용한 가정
- 문제팩에서 수정하거나 제외한 문항

### 3. 교재 문제팩 제작

- OCR 복원이 확정된 문항은 먼저 `교재 문제팩`으로 만든다.
- 교재 문제팩의 기본문제 ID는 `[단원약어]-TXT-001`처럼 `TXT`를 포함한다. 예: `INV-TXT-001`.
- 최상위 `source`와 모든 문항의 `source`는 반드시 `교재 문제팩`으로 통일한다.
- 교재용은 복원 원문을 보존하는 목적이므로, 별도 응용문제는 기본적으로 넣지 않는다. 필요할 때만 `variants`를 추가한다.
- 원문의 선택지 일부가 훼손되어 정답을 확정할 수 없다면 임의로 교재 문제팩에 넣지 않는다. 정확한 원본 이미지가 있거나, 문맥상 단 하나로 복원되는 근거가 있을 때만 넣는다.

### 4. AI 제작 기본문제

- 원문 핵심 개념과 회계처리를 유지한다.
- 전산회계 1급 자격시험의 회계원리·재무회계 범위에 맞춘다.
- 기본문제는 원문을 이해했는지 확인하도록 만든다.
- 보기 4개, 정답 1개, 해설 1개를 원칙으로 한다.
- 계산문제는 금액·단위·기간을 명확히 표시한다.
- 문제에 전제가 필요하면 반드시 문제 안에 적는다. 예: 단기매매증권 거래수수료는 당기비용으로 처리한다.

### 5. AI 제작 변형 응용문제

- 기본문제와 같은 개념을 확인하되 상황, 수치, 질문 방향 또는 보기 배열을 바꾼다.
- 단순히 숫자만 바꾸지 말고 실제 시험에서 판단할 수 있도록 한 단계 응용한다.
- 변형문제는 기본문제의 `parentId`와 연결한다.
- 기본문제와 변형문제의 정답이 서로 충돌하지 않는지 다시 계산한다.
- 기본 12문항 + 변형 12문항을 기본값으로 하되, 사용자가 다른 문항 수를 지정하면 그 수를 따른다.

## 문제팩 JSON 기준

파일명 예시(모두 `문제펙` 폴더 안에 저장):

`[단원명]_문제팩_[출처].json`

예:

`재고자산_문제팩_GPT.json`

교재용 파일은 다음처럼 저장한다.

`[단원명]_문제팩_교재.json`

최상위 구조는 다음과 같다.

```json
{
  "format": "회계원리 학습센터 문제팩",
  "version": "1.0",
  "encoding": "UTF-8",
  "chapter": "재고자산",
  "createdAt": "2026-07-26",
  "examLevel": "전산회계 1급",
  "source": "GPT 문제팩",
  "questions": [],
  "variants": []
}
```

각 기본문제에는 다음 필드를 사용한다.

- `id`: 기존 ID와 겹치지 않는 고유 ID. 예: `INV-GPT-001`
- `chapter`: 단원명
- `number`: 기본문제 번호
- `type`: 큰 유형
- `concept`: 세부 개념
- `source`: 문제팩 출처 표시명. 예: `GPT 문제팩`, `Claude 문제팩`
- `variant`: 기본문제는 `false`
- `q`: 질문
- `o`: 보기 4개 배열
- `a`: 정답 보기의 0부터 시작하는 번호. 0=①, 1=②, 2=③, 3=④
- `e`: 해설
- `case`: 필요한 경우에만 사용하는 추가 사례 문장
- `table`: 거래내역·계산표·분개표처럼 행과 열로 읽어야 하는 자료에 사용하는 선택 필드. `headers`와 `rows`를 넣으면 학습센터가 문제 안에 표로 표시한다.

표가 필요한 문제는 OCR 텍스트에 표 내용을 줄글로 붙여 넣지 말고, 다음 형식으로 `table`을 사용한다. 원본에 표·영수증·전표 이미지가 있으면 함께 대조하여 항목과 숫자를 정확히 옮긴다.

```json
"table": {
  "headers": ["일자", "구분", "수량", "단가"],
  "rows": [
    ["10월 1일", "기초재고", "10개", "개당 100원"],
    ["10월 8일", "매입", "30개", "개당 110원"]
  ]
}
```

표가 없으면 `table` 필드는 생략한다. 표 안의 문구도 한글 UTF-8로 저장한다.

### 출처 표시 필수 규칙

학습센터의 출처 선택은 **문제의 난이도·제작 단계가 아니라 문제팩 자체를 구분**하기 위한 기능이다.

- 최상위 JSON에 문제팩의 단일 출처를 `source`로 적는다. 예: `"source": "Claude 문제팩"`
- 기본문제와 변형문제의 각 `source`도 모두 최상위 출처명과 똑같이 쓴다.
- `교재 OCR 복원`, `교재 OCR 응용`, `기본문제`, `변형문제`처럼 제작 단계를 `source`에 쓰지 않는다. 이러한 정보는 `variant`, `type`, `concept` 또는 검토 문서에서 구분한다.
- 파일명·ID·출처명을 같은 제작자 기준으로 맞춘다. 예: 파일명 `재고자산_문제팩_Claude.json`, 기본 ID `INV-CLA-001`, 변형 ID `INV-CLA-V001`, 출처 `Claude 문제팩`.
- 하나의 JSON에는 하나의 문제팩 출처만 넣는다. 다른 AI가 만든 문제는 별도 JSON으로 저장한다.
- 교재용의 고정 출처명은 `교재 문제팩`이다. 파일명은 `[단원명]_문제팩_교재.json`, 기본문제 ID는 `[단원약어]-TXT-001` 형식으로 한다.
- GPT 제작용의 고정 출처명은 `GPT 문제팩`이다. 파일명은 `[단원명]_문제팩_GPT.json`, 기본문제 ID는 `[단원약어]-GPT-001`, 변형문제 ID는 `[단원약어]-GPT-V001` 형식으로 한다.
- Claude 제작용의 고정 출처명은 `Claude 문제팩`이다. 파일명은 `[단원명]_문제팩_Claude.json`, 기본문제 ID는 `[단원약어]-CLA-001`, 변형문제 ID는 `[단원약어]-CLA-V001` 형식으로 한다.

각 변형문제에는 기본문제 필드에 더해 다음을 사용한다.

- `id`: 기본문제와 겹치지 않는 고유 ID. 예: `INV-GPT-V001`
- `parentId`: 연결된 기본문제 ID
- `variant`: `true`

## 저장 전 검증 목록

문제팩을 저장하기 전에 반드시 확인한다.

- UTF-8 JSON으로 정상 파싱되는가
- `format`이 `회계원리 학습센터 문제팩`인가
- 단원명과 시험 수준이 올바른가
- 기본문제와 변형문제의 ID가 모두 고유한가
- 모든 문항에 보기 4개가 있는가
- 정답 번호가 0~3 범위인가
- 모든 문항에 질문과 해설이 있는가
- 변형문제의 `parentId`가 실제 기본문제 ID와 연결되는가
- 계산문제의 정답과 해설 계산이 일치하는가
- 기존 문제팩과 ID가 겹치지 않는가
- 최상위 `source`가 기록되어 있는가
- 기본문제와 변형문제의 모든 `source`가 최상위 `source`와 동일한가
- `교재 OCR 복원`·`교재 OCR 응용`처럼 제작 단계를 출처명으로 사용하지 않았는가
- 교재용과 AI 제작용을 서로 다른 JSON으로 만들었는가
- 교재용의 최상위·문항별 `source`가 모두 `교재 문제팩`인가
- 교재용은 OCR 복원검토 문서의 정답·가정과 일치하는가

## OCR 검토 문서를 다른 AI와 함께 사용할 때

OCR가 많이 훼손된 단원은 먼저 OCR 복원 검토 문서를 만든 뒤, 원문 OCR·원본 이미지·검토 문서를 함께 다른 AI에 전달하는 방식이 안전하다.

1. 원본 OCR 텍스트와 가능한 원본 이미지로 `단원명_OCR_복원검토.md`를 만든다.
2. Claude에는 원본 OCR, 원본 이미지, 복원 검토 문서와 이 제작 기준 문서를 모두 제공한다.
3. Claude에게 `교재 문제팩`과 `Claude 문제팩`을 별도 JSON으로 만들고, 각 JSON의 최상위·문항별 `source`를 각각 하나로 통일하라고 요청한다.
4. Claude에게 검토 문서를 참고하되 원문·이미지와 불일치하는 부분은 따로 표시하고, 확정할 수 없는 문항은 임의로 만들지 말라고 요청한다.
5. Claude가 만든 JSON은 학습센터에 넣기 전에 다시 정답·계산·ID·출처를 검토한다.

복원 검토 문서는 정답을 보장하는 자료가 아니라, OCR 오류를 줄이기 위한 **검토 기준**이다. 원본 이미지가 있다면 항상 이미지가 최우선이다.

## 학습센터 가져오기 안내

1. `백업·복원` 메뉴로 이동한다.
2. `문제펙` 폴더의 문제팩 JSON을 `새 단원 문제팩 가져오기`에서 선택한다.
3. `문제팩 추가`를 누른다.
4. `문제 풀기`에서 단원을 선택한다.
5. 출처를 고르지 않으면 통합 학습, 출처를 고르면 특정 문제팩 학습이 된다.

문제팩 JSON은 학습기록 백업 JSON과 다르다. 문제팩은 `새 단원 문제팩 가져오기`에서 사용하고, 학습 후 만든 백업 파일은 `JSON 백업·복원`에서 사용한다.

## 작업 원칙

- 기존 학습기록과 기존 문제팩을 삭제하거나 덮어쓰지 않는다.
- 새 문제팩은 기존 ID와 겹치지 않게 만든다.
- 문제팩 JSON과 OCR 복원검토 문서는 항상 `문제펙` 폴더에 저장한다.
- 학습기록 JSON은 사용자가 수동으로 `학습기록` 폴더에 보관한다.
- HTML 수정은 사용자가 별도로 요청한 경우에만 한다.
- OCR이 불확실한 문항은 검토 문서에 남기고, 정답이 확정되지 않으면 문제팩에서 제외한다.
- 모든 문제는 현재 전산회계 1급 시험 대비에 적합한 난이도와 용어로 작성한다.
