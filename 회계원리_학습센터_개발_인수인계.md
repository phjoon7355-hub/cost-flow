# 회계원리 학습센터 개발 인수인계

- 작성일: 2026-07-25
- 최종 업데이트: 2026-07-28
- 실제 사용 파일: `C:\Users\MyPC\Documents\Codex\2026-07-23\ocr-html-01-500-000-02\outputs\회계원리 학습센터\회계원리_학습센터.html`
- 현재 버전: **v2.3**
- 목적: 새 Codex 대화에서 사용자가 처음부터 배경을 다시 설명하지 않아도, 이 문서와 HTML을 읽고 개발을 이어갈 수 있게 한다.

---

## 1. 새 대화의 Codex가 가장 먼저 알아야 할 것

1. 위 경로의 `회계원리_학습센터.html`이 현재 사용 중인 **기준 파일(source of truth)** 이다.
2. 작업 전 반드시 이 Markdown 문서와 실제 HTML을 함께 읽고 현재 상태를 확인한다.
3. GitHub Pages·휴대폰 연동·클라우드 실시간 동기화는 논의했지만 **현재는 보류**했다.
4. 사용자는 당분간 `outputs\회계원리 학습센터`의 로컬 HTML을 Google Chrome에서 열어 학습한다.
5. 기존의 별도 테스트용 HTML 문제집은 학습센터와 별개이며 앞으로 무시해도 된다.
6. 기존 학습기록을 보호해야 하므로 `localStorage` 키와 문제 ID를 임의로 변경하지 않는다.
7. 수정 전에는 사용자에게 JSON 학습기록 백업을 권장하고, 수정 후에는 한글·채점·기록·복원 동작을 검증한다.

### 현재 폴더 구조

```text
회계원리 학습센터
├─ 문제펙                 ← 문제팩 JSON·OCR 복원검토 문서
├─ 학습기록               ← 사용자가 수동으로 옮겨 보관한 학습기록 JSON
├─ 회계원리_학습센터.html
├─ 문제팩_제작_기준.md
└─ 회계원리_학습센터_개발_인수인계.md
```

새 교재·GPT·Claude 문제팩과 OCR 검토 문서는 반드시 `문제펙` 폴더에 저장한다. 학습센터의 `JSON 저장`은 Chrome 다운로드 기능이므로 저장 위치를 자동 지정하지 않으며, 사용자가 내려받은 뒤 `학습기록` 폴더로 수동 이동한다.

---

## 2. 왜 이 학습센터를 만들었는가

사용자는 전산회계 1급을 공부하면서 단원별 연습문제를 계속 풀 예정이다. 단순히 문제를 한 번 풀고 끝내는 것이 아니라 다음 과정을 누적하고 싶어 했다.

- 틀린 문제 자동 기록
- 틀린 문제의 단원·유형·세부 개념 분류
- 오답만 다시 풀기
- 동일 개념을 조금 바꾼 변형문제로 재확인
- 연속 정답을 통한 숙달 판정
- 단원별 기록을 계속 쌓아 전체 취약 유형 확인
- 브라우저 기록이 사라질 경우를 대비한 JSON 백업·복원
- 교재의 새 단원을 문제팩 JSON으로 계속 추가

이 요구를 충족하기 위해 외부 설치가 필요 없는 단일 HTML 기반의 개인 학습 앱을 만들었다.

---

## 3. 현재 구현 상태

### 기본 구성

- 단일 실행 파일: `회계원리_학습센터.html`
- 문자 인코딩: UTF-8
- 외부 라이브러리·서버 불필요
- Google Chrome에서 로컬 파일로 실행
- 반응형 화면으로 PC와 작은 화면을 지원

### 내장 문제

- 단원: `기초이론`
- 기본문제: 12문항
- 변형문제: 12문항
- 총 내장 문제: 24문항
- 기본문제 ID: `BASIC-001` ~ `BASIC-012`
- 변형문제 ID: `BASIC-V001` ~ `BASIC-V012`
- 변형문제는 `parentId`로 원본 기본문제와 연결된다.

### 출제 방식

- 5문항: 빠른 복습
- **10문항: 권장값**
- 20문항: 집중 점검
- 가능한 문제 모두
- 전체 문제 방식에서는 기본문제와 변형문제를 혼합한다.
- 권장 10문항은 대략 기본 6문항과 변형 4문항이 섞이도록 설계했다.
- 같은 원본 유형이 한 세션에서 불필요하게 중복되지 않도록 균형 출제한다.

### 주요 화면

- `학습 현황`: 누적 풀이, 정답률, 복습 필요, 숙달 완료, 유형별 숙달도
- `유형별 숙달도`: `chapter`를 부모 폴더로 묶어 단원별 전체 정답률과 세부 유형 그래프를 표시한다. `모두 닫기`·`모두 보기` 버튼과 부모 폴더별 열림 상태 저장을 지원한다. 내장 단원 `기초이론`은 화면에서 `회계 이론`으로 표시한다.
- `빠른 복습`: 유형별 숙달도에서 정답률 70% 미만이며 복습할 문제가 남은 유형 이름을 누르면, 틀린 문제·취약 유형·변형문제·10문항 점검을 바로 시작할 수 있는 선택창이 열린다.
- `추천 복습`: 단원별 추천 복습 항목은 버튼이다. 누르면 해당 단원의 `틀린 문제만`이 남아 있는 문항 수만큼 즉시 시작된다.
- `문제 풀기`: 단원·학습 방식·문항 수 선택 및 채점
- `문제 안의 표`: 문제 객체에 `table.headers`와 `table.rows`가 있으면 거래내역·계산표를 문제 본문 안에 표로 표시한다. 작은 화면에서는 가로로 밀어 볼 수 있다.
- `추천 학습 순서`: 학습 세션 만들기 아래에서 교재 전체 → AI 응용 전체 → 오답 복습 → 취약 유형 → 변형문제 → 통합 점검의 순서를 확인할 수 있다.
- `풀이 상태`: 정답 확인 전에 `확실함 · 헷갈림 · 찍음`을 선택할 수 있다. `헷갈림·찍음`을 선택하면 즉시 이해 메모칸이 열리며, 정답·오답과 관계없이 함께 저장된다. `헷갈림·찍음`으로 맞힌 문제는 정답률에 반영하지 않고 `이해 보류`로 남긴다.
- `이전 문제`: 세션 안에서 이미 채점한 이전 문제의 답·해설을 다시 볼 수 있다. 다시 채점되지는 않으므로 풀이 기록이 중복되지 않는다.
- `다시 풀기`: 채점 뒤 해당 문제의 기존 풀이 기록을 안전하게 되돌리고 같은 문제를 다시 풀 수 있다. 처음에 `확실함`으로 잘못 표시한 경우에도 상태를 고쳐 기록할 수 있다.
- `문제팩 출처`: 통합 학습 또는 특정 문제팩 출처를 선택할 수 있으며, 문제와 오답노트에 출처를 표시한다.
- `오답노트`: 누적 오답과 숙달 상태 확인
- `백업·복원`: 화면 안의 패널 순서는 `새 단원 문제팩 가져오기` → `JSON 백업·복원` → `데이터 관리 원칙`이다. 문제팩을 먼저 추가하고, 학습 후 JSON 기록을 저장·복원하도록 배치했다.

---

## 4. 오답 및 숙달 판정 원칙

문제를 채점할 때마다 자동으로 다음 내용을 기록한다.

- 문제 ID
- 실제 출제된 문제 ID
- 단원
- 유형
- 세부 개념
- 정답 여부
- 풀이 시각
- 변형문제 여부

오답이 발생하면 해당 기본문제의 연속 정답 횟수가 0으로 초기화된다.

- 오답 경험이 있고 연속 정답이 2회 미만: `복습 필요`
- 오답 경험 후 연속 2회 이상 정답: `숙달`
- 숙달되어도 과거 오답 이력은 삭제하지 않는다.

변형문제를 풀었을 때도 기록은 `parentId`를 통해 해당 기본 개념에 누적된다.

`헷갈림` 또는 `찍음`으로 맞힌 경우에는 실제 선택지는 정답이어도 학습상 정답으로 처리하지 않는다. 해당 문제는 `이해 보류`로 남고 ‘틀린 문제만’ 복습 대상에 포함된다. 이후 확실히 맞힌 기록을 숙달 기준 횟수만큼 쌓으면 이해 보류가 해제된다.

---

## 5. 내부 저장 구조

브라우저 저장소 키:

```text
accountingStudyCenter_v1
```

주요 상태 구조:

```text
state
├─ version       앱 데이터 버전
├─ questions     기본문제 배열
├─ variants      변형문제 배열
├─ progress      문제별 누적 정답·오답·연속 정답 상태
├─ attempts      개별 풀이 이력
└─ settings      숙달 기준 등 설정
```

현재 앱 버전은 `APP_VERSION = "2.3"`이다.

### 중요한 유지보수 원칙

- `APP_KEY`를 바꾸면 기존 브라우저 기록을 찾지 못하므로 변경하지 않는다.
- 기존 문제의 의미가 같은 한 ID를 바꾸지 않는다.
- 다른 개념의 문제로 교체할 때 기존 ID를 재사용하지 않는다.
- 데이터 구조를 변경할 경우 이전 버전을 읽는 마이그레이션 또는 `normalizeState()` 호환 처리를 추가한다.
- HTML 파일을 같은 위치와 같은 이름으로 덮어쓰면 Chrome의 기존 저장 기록을 이어갈 수 있도록 유지한다.

---

## 6. JSON 파일은 두 종류다

### 6.1 학습기록 백업 JSON

학습센터의 `JSON 저장` 버튼으로 사용자가 직접 내려받은 뒤 `학습기록` 폴더에 보관한다.

문제는 `정답 확인` 후 오답 원인 또는 이해 메모 처리까지 완료하면 Chrome 내부 저장소에 즉시 누적된다. 정상적인 절전 모드 후에도 이 기록은 유지되지만, 아직 채점하지 않은 현재 문제의 선택 상태는 저장되지 않을 수 있다.

포함 내용:

- 문제은행
- 변형문제
- 모든 풀이 이력
- 문제별 진행 상태
- 숙달도
- 설정

복원 방식:

- `기록 합치기`: 현재 기록을 유지하면서 JSON 기록을 더한다. 동일한 풀이 기록은 중복 제거한다.
- `백업으로 교체`: 현재 브라우저 기록을 버리고 선택한 JSON 저장 당시 상태로 완전히 되돌린다.

브라우저 데이터 삭제에 대비하여 단원 학습이 끝날 때마다 한글 파일명으로 백업하는 것을 권장한다. 오래 학습하는 날에는 식사·휴식 전, 또는 1~2시간 학습 후에도 중간 백업을 권장한다.

각 백업은 저장 시점까지의 문제은행·풀이 이력·숙달도를 모두 포함한 누적 파일이다. 따라서 가장 마지막 백업만 있어도 복원에는 충분하고, 중간 백업은 안전장치 역할을 한다.

Chrome은 같은 파일명으로 다시 저장해도 보통 기존 파일을 덮어쓰지 않고 `(1)`, `(2)`를 붙인 새 파일을 만든다. 중간 백업은 시간대를 붙여 구분한다.

예:

```text
학습기록\기초이론 학습기록 260724.json
학습기록\당좌자산 학습기록 260726.json
학습기록\회계원리 학습기록 260728 오전.json
학습기록\회계원리 학습기록 260728 마무리.json
```

### 6.2 새 단원 문제팩 JSON

새로운 학습문제를 추가하기 위한 파일이다. 학습기록 백업과 다른 형식이다.

필수 최상위 구조:

```json
{
  "format": "회계원리 학습센터 문제팩",
  "version": "1.0",
  "encoding": "UTF-8",
  "chapter": "단원명",
  "examLevel": "전산회계 1급",
  "questions": [],
  "variants": []
}
```

각 기본문제의 주요 필드:

```text
id, chapter, number, type, concept, q, o, a, e
```

- `o`: 보기 4개 배열
- `a`: 정답 보기의 0 기반 번호(0~3)
- `e`: 정답 해설

각 변형문제에는 기본문제 ID를 가리키는 `parentId`와 `variant: true`가 추가된다.

문제팩은 `문제펙` 폴더의 JSON을 학습센터 다음 메뉴에서 불러온다.

```text
백업·복원 → 새 단원 문제팩 가져오기 → 문제팩 추가
```

가져온 문제팩은 해당 Chrome의 내부 저장소에 병합되며 기존 오답과 풀이 기록은 유지된다.

문제팩 JSON과 학습기록 JSON은 서로 다른 파일이다. 문제팩은 `문제펙`에, 학습기록은 `학습기록`에 보관한다.

---

## 7. 앞으로 새 단원을 추가하는 표준 절차

사용자가 새 대화에서 다음 중 하나를 제공한다.

1. 교재의 단원별 연습문제를 복사하여 제공
2. OCR로 손상된 문제 텍스트 제공
3. 자료 없이 단원명과 범위만 알려 주고 문제 제작 요청

Codex는 다음 순서로 처리한다.

1. OCR 오류와 잘못 인식된 회계 용어를 교정한다.
2. 원문 문제의 정답과 출제 의도를 검토한다.
3. 현재 시행되는 **전산회계 1급 자격시험 수준**을 넘지 않도록 범위를 제한한다.
4. 대학 중급회계·재경관리사 수준으로 불필요하게 확장하지 않는다.
5. 정답이 하나만 나오도록 조건과 표현을 명확히 한다.
6. 기본문제와 변형문제를 함께 만든다.
7. 각 문제에 단원·유형·세부 개념 태그와 고유 ID를 부여한다.
8. UTF-8 한글 문제팩 JSON을 생성한다.
9. JSON 구조, 보기 4개, 정답 인덱스, 중복 ID, `parentId` 연결을 검증한다.
10. 사용자가 학습센터에서 문제팩을 가져와 권장 10문항을 풀도록 안내한다.

### 문제 제작 품질 원칙

- 교재 문장을 그대로 복사하는 대신 상황·수치·질문 방향을 바꿔 응용한다.
- 단순 보기 순서 변경만으로 변형문제를 만들지 않는다.
- 전산회계 1급 수준에서 학습 가치가 있는 함정 보기를 사용한다.
- 회계기준이나 전제에 따라 정답이 달라질 수 있는 문제는 조건을 보완하거나 제외한다.
- 정답과 해설의 계산 및 회계처리를 별도로 검증한다.
- 한글이 깨지지 않도록 UTF-8로 저장한다.

---

## 8. 현재 보류된 기능과 제한사항

### GitHub 및 휴대폰 연동

GitHub Pages 배포와 휴대폰 접속을 시험했으나, 사용자는 당분간 이를 보류하고 `outputs\회계원리 학습센터`의 로컬 HTML만 사용하기로 했다.

현재 사용본 v2.3에는 다음이 포함되지 않는다.

- GitHub의 `manifest.json` 자동 문제팩 로딩
- 휴대폰과 PC 간 학습기록 자동 동기화
- 로그인
- 클라우드 데이터베이스

### 기기 간 기록 공유

브라우저 내부 저장소는 기기와 Chrome 프로필별로 분리된다. 다른 기기로 기록을 옮기려면 현재는 학습기록 JSON을 저장하고 다른 환경에서 복원해야 한다.

### 기존 테스트 HTML

초기에 만든 `기초이론_응용문제.html`, `자본_응용문제.html` 등 별도 HTML은 테스트용이다. 학습센터의 누적 기록과 연결하지 않으며 앞으로의 기준 파일로 사용하지 않는다.

---

## 9. HTML을 수정할 때의 안전 절차

1. 실제 작업 폴더의 HTML 버전과 파일 크기, 수정일을 확인한다.
2. 사용자가 최신 학습기록 JSON을 백업했는지 확인한다.
3. HTML의 기존 동작과 저장 구조를 먼저 읽는다.
4. 별도의 작업 사본에서 수정한다.
5. JavaScript 문법 검사를 한다.
6. 실제 브라우저에서 다음을 검증한다.
   - 학습센터 로딩
   - 한글 표시
   - 권장 10문항 출제
   - 기본문제와 변형문제 혼합
   - 채점 및 해설
   - 오답 자동 기록
   - 연속 정답 숙달 처리
   - 오답만 다시 풀기
   - 문제팩 가져오기
   - 한글 이름의 JSON 백업
   - 기록 합치기와 백업 교체
7. 검증된 파일만 실제 D 폴더의 같은 이름으로 교체한다.
8. 기존 학습기록이 유지되는지 마지막으로 확인한다.

학습기록 JSON은 Chrome 다운로드 후 사용자가 `학습기록` 폴더로 옮겼는지 확인한다. HTML이 다운로드 폴더를 자동으로 바꾸도록 수정하지 않는다.

---

## 10. 새 대화에서 사용할 요청문 예시

아래 문장을 새 Codex 대화의 첫 메시지로 사용하면 된다.

```text
C:\Users\MyPC\Documents\Codex\2026-07-23\ocr-html-01-500-000-02\outputs\회계원리 학습센터 폴더의
`회계원리_학습센터_개발_인수인계.md`와
`회계원리_학습센터.html`을 먼저 읽어줘.

이 파일이 현재 실제 사용 중인 기준 파일이야.
기존 학습기록과 문제 ID를 보존하면서 작업해줘.
현재는 GitHub·휴대폰·클라우드 동기화를 보류하고 로컬 HTML만 사용하고 있어.
작업 전에 현재 상태와 변경 계획을 먼저 알려줘.
```

새 단원 문제팩을 만들 때는 이어서 다음처럼 요청한다.

```text
새 단원은 `[단원명]`이야.
아래 교재 연습문제를 OCR 오류까지 검토한 후,
현재 전산회계 1급 수준의 기본문제와 변형문제로 만들어줘.
완성 결과는 이 학습센터에서 가져올 수 있는 UTF-8 한글 문제팩 JSON으로 제공해줘.
```

---

## 11. 현재 폴더의 보조 파일

- `사용안내.txt`: 사용자용 간단한 사용법
- `문제팩_형식_예시.json`: 개발 참고용 문제팩 구조 예시. 실제 학습용으로 가져오지 않는다.
- `회계원리_학습센터_개발_인수인계.md`: 새 대화와 향후 개발을 위한 본 문서
- `문제펙\`: 실제 문제팩 JSON과 OCR 복원검토 문서
- `학습기록\`: 사용자가 보관한 학습기록 JSON

---

## 12. 핵심 요약

이 학습센터는 전산회계 1급 단원별 문제를 누적하고, 오답·이해 보류·취약 유형을 장기간 추적하며, 변형문제로 숙달할 수 있도록 만든 로컬 학습 앱이다. 현재 기준은 `outputs\회계원리 학습센터`의 v2.3 HTML이며, 로컬 Chrome 저장소와 JSON 백업을 사용한다. 새 단원은 전산회계 1급 수준의 UTF-8 문제팩 JSON으로 추가한다. 기존 기록·문제 ID·한글 인코딩을 보존하는 것이 최우선이다.

### v2.3 반영 사항

- 문제 안의 거래내역·계산표를 `table.headers`와 `table.rows`로 표시한다.
- `헷갈림`·`찍음` 선택 시 즉시 이해 메모를 기록하며, `다시 풀기`로 채점 기록을 되돌린 뒤 같은 문제를 다시 풀 수 있다.
- 유형별 숙달도에서 취약 유형을 누르면 빠른 복습 선택창이 열린다.
- 추천 복습의 단원 항목은 버튼이며, 누르면 해당 단원의 복습 필요 문제를 모두 바로 시작한다.
